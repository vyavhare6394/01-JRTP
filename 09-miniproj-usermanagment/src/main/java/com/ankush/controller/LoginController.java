package com.ankush.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestParam;

import com.ankush.entity.UserEntity;
import com.ankush.model.LoginUsr;
import com.ankush.service.UserManagmentServiceImpl;
import com.ankush.service.UserMangmentService;

@Controller
public class LoginController {
	
	//@Autowired
	//private UserMangmentService service;
	
	
	
	/*
	 * @GetMapping("/login") public String loginUser(Model theModel) {
	 * 
	 * 
	 * LoginUsr user=new LoginUsr();
	 * 
	 * theModel.addAttribute("user", user);
	 * 
	 * return "login-form";
	 * 
	 * }
	 * 
	 * @PostMapping("/LoginPost") public String
	 * sbmitLogin(@RequestParam("email")String email) {
	 * 
	 * UserEntity existing = service.getByUserEmail(email);
	 * 
	 * if(existing!=null) {
	 * 
	 * 
	 * 
	 * }
	 */	

		
		
		
//		return "login-success";
		
	}
	
	
	
	
	


